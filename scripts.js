function increment() {
    var string = prompt("Write Something")
    var lastElement = string.charAt(string.length-1);
    alert (string+lastElement.length);
    if (lastElement == true) {
      return string.indexOf++;
    }
  }
  
  increment();